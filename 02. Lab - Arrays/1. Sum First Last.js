arr = ['1','2','3','4','5','6','10','11'];

function SumFirstLast(arr){
const first = Number(arr[0]);
const last = Number(arr[arr.length -1]);

return result = Number(first + last);
}

console.log(SumFirstLast(arr));